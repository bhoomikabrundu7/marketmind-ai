# AI Stock Market Dashboard

A Streamlit dashboard for stock overviews, price prediction, technical indicators, news sentiment, and risk analysis.

## Setup

```bash
pip install -r requirements.txt
streamlit run app.py
```

Place downloaded market data in `data/raw/` and processed model-ready data in `data/processed/`.
